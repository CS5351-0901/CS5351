WEEK4_CATCH_UP
Created during Week 4 to document an earlier course stage.
This file/commit uses the real current Git date and does not claim that the file existed earlier.

# A1 role and scope

A1 owns the plugin metadata, configuration, and environment boundary:

- `metadata.yaml`
- `_conf_schema.json`
- `pyproject.toml`
- Configuration reads in `main.py`
- Node.js and JavaScript checker dependency preparation functions in `main.py`

Week 1–4 personal branch commits provide role evidence only under `course_evidence/A1/**`. They do not change project business source.
